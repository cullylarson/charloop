module.exports = function(id, title, folder, tracks, createdStamp, modifiedStamp) {
    return {
        id,
        title,
        folder,
        tracks,
        createdStamp,
        modifiedStamp,
    }
}
