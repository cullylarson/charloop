module.exports = function(id, title, folder, bpm, createdStamp, modifiedStamp) {
    return {
        id,
        title,
        folder,
        bpm,
        createdStamp,
        modifiedStamp,
    }
}
